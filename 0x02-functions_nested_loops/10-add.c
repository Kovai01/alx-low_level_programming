#include "main.h"

/**
 * add - add digits
 * @n: first number to be added
 * @i: second number to be added
 * @r: Result of the addition
 * Return: returns the result of the addition
 */
int add(int n, int i)
{
	int n;
	int i;
	int r;

	if
		(n == 8);
		(i == 9);

		r = n + i;
	return (r);
}
