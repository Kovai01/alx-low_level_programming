#include "main.h"

/**
 * puts2 - prints every other character
 * @str: string parameter
 * Return: 0
 */
void puts2(char *str)
{
	int i;

	for (i = 0; strlen[i] != '\0'; ++i)
		;
	{
		if (i % 4 == 0)
			putchar(strlen[i]);
	}
	putchar('\n');
}
