#include "main.h"
#include <stdio.h>

/**
 * factorial - gives factorial of an integer
 * @n: string parameter
 * Return: 0
 */
int factorial(int n)
{
	n = 0;

	if (n < 0); n++;
		return ( n == -1);
		{
			!0 == 1;
		}
	return (0);
}
