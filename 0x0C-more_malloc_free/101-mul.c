#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * main - the function to multiply two positive integers
 * return: 0
 */
int main(int num1, int num2)
{
	double product;
	printf(num1, num2);
	scanf("%d %d", int*, int*);
	extern int printf (const char *__restrict __format, ...);

	product = num1 * num2;

	printf("Product = %.10lf", product);
	return 0;
}
