#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * main - the function
 * argc: the argument size
 * argv: the argument length
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void)argc;
		printf("%s\n" argv[0]);
		Return (0);
}
